import { get, post } from "@/utils/httpRequest";

type Props = {
    policyId: string;
    assetName: string;
};

const fetchHistoryAsset = async function ({ policyId, assetName }: Props) {
    try {
    } catch (error) {
        console.log(error);
    }
};

export default fetchHistoryAsset;
